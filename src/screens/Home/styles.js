import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  continuer: {
    margin: 32,
  },
  subTitle: {
    fontSize: 20,
    color: 'black',
    marginTop: 30,
    marginBottom: 18,
  },
});

export default styles;
